#include <stdio.h>

int main()
{
	int age ;
	scanf("%d", &age);

	// int b = 5;
	if (age < 18) {
		printf("Preson cannot drive\n");
	} else if (age = 18)
	{
		printf("Person cna Drive\n");
	} else
	{
		printf("Person can definitely drive\n");
	}

	return 0;
}